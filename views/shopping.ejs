<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Shop - Supermarket</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <style>
    body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; }
    .navbar { box-shadow: 0 2px 8px rgba(0,0,0,0.1); }
    .search-bar { margin: 30px 0; }
    .search-bar input { border-radius: 8px; border: 2px solid #e0e0e0; padding: 12px; }
    .search-bar button { border-radius: 8px; }
    .product-card { border: none; border-radius: 12px; box-shadow: 0 2px 12px rgba(0,0,0,0.08); transition: 0.3s; overflow: hidden; }
    .product-card:hover { box-shadow: 0 8px 24px rgba(0,0,0,0.15); }
    .product-card img { height: 200px; object-fit: cover; }
    .product-card .card-body { padding: 16px; }
    .product-card .card-title { font-weight: 600; color: #1a1a1a; }
    .product-card .price { font-size: 1.3rem; color: #4169e1; font-weight: 700; margin: 10px 0; }
    .product-card .btn { border-radius: 6px; width: 100%; }
    .qty-selector { width: 80px; }
  </style>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-light bg-white sticky-top">
  <div class="container">
    <a class="navbar-brand fw-bold" href="/">🛒 Supermarket</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item"><a class="nav-link" href="/contact">Contact</a></li>
        <li class="nav-item"><a class="nav-link" href="/shopping">Shopping</a></li>
        <li class="nav-item"><a class="nav-link" href="/cart"><i class="fas fa-shopping-cart"></i> Cart</a></li>
        <li class="nav-item"><a class="nav-link" href="/logout">Log out</a></li>
      </ul>
    </div>
  </div>
</nav>

<div class="container py-4">
  <% if (messages && messages.length) { %>
    <div class="alert alert-info alert-dismissible fade show" role="alert">
      <%= messages.join('<br>') %>
      <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
  <% } %>

  <div class="search-bar">
    <form action="/search" method="GET" class="d-flex gap-2">
      <input type="text" name="q" class="form-control" placeholder="Search products..." value="">
      <button type="submit" class="btn btn-primary">Search</button>
    </form>
  </div>

  <h2 class="mb-4">All Products</h2>

  <div class="row g-3">
    <% if (products && products.length) { %>
      <% products.forEach(p => { %>
        <div class="col-md-4 col-lg-3">
          <div class="card product-card h-100">
            <% if (p.image) { %>
              <img src="/images/<%= p.image %>" class="card-img-top" alt="<%= p.productName %>">
            <% } else { %>
              <div class="card-img-top bg-light d-flex align-items-center justify-content-center" style="height:200px">No Image</div>
            <% } %>
            <div class="card-body d-flex flex-column">
              <h5 class="card-title"><%= p.productName %></h5>
              <p class="price">$<%= parseFloat(p.price||0).toFixed(2) %></p>
              <p class="text-muted mb-2"><i class="fas fa-box"></i> Stock: <strong><%= p.quantity || 0 %></strong></p>

              <form action="/add-to-cart/<%= p.id %>" method="POST" class="mt-auto d-flex gap-2">
                <select name="qty" class="form-select form-select-sm qty-selector">
                  <% for (let i=1;i<=Math.min(10, (p.quantity||0)); i++) { %>
                    <option value="<%= i %>"><%= i %></option>
                  <% } %>
                </select>
                <button class="btn btn-primary flex-grow-1" <%= (p.quantity||0) <= 0 ? 'disabled' : '' %>>
                  <i class="fas fa-cart-plus"></i> Add to Cart
                </button>
              </form>
            </div>
          </div>
        </div>
      <% }) %>
    <% } else { %>
      <div class="col-12">
        <div class="alert alert-info">No products available</div>
      </div>
    <% } %>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
