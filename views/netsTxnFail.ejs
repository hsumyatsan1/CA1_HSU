<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Transaction Failed</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --fail-bg: #f6f7fb;
            --fail-card: #ffffff;
            --fail-ink: #1f2330;
            --fail-muted: #6b7280;
            --fail-accent: #ef4444;
            --fail-line: #e5e7eb;
        }

        body {
            font-family: "Manrope", "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
            background: radial-gradient(900px 400px at 10% 0%, #ffe4e6 0%, transparent 60%),
                        radial-gradient(800px 300px at 100% 0%, #e8edff 0%, transparent 65%),
                        var(--fail-bg);
            color: var(--fail-ink);
        }

        .fail-shell {
            min-height: 100vh;
            display: grid;
            place-items: center;
            padding: 32px 16px 48px;
        }

        .fail-card {
            background: var(--fail-card);
            border-radius: 20px;
            padding: 28px 24px;
            box-shadow: 0 24px 50px rgba(17, 24, 39, 0.12);
            border: 1px solid var(--fail-line);
            max-width: 520px;
            width: 100%;
            text-align: center;
        }

        .fail-icon {
            width: 84px;
            height: 84px;
            margin: 0 auto 14px;
            display: grid;
            place-items: center;
            border-radius: 22px;
            background: rgba(239, 68, 68, 0.12);
            color: var(--fail-accent);
            font-size: 2.4rem;
            font-weight: 700;
        }

        .fail-title {
            font-weight: 700;
            font-size: 1.5rem;
            margin-bottom: 6px;
        }

        .fail-sub {
            color: var(--fail-muted);
            margin-bottom: 18px;
        }

        .fail-message {
            background: #fff5f5;
            border: 1px solid rgba(239, 68, 68, 0.25);
            color: #b91c1c;
            padding: 10px 12px;
            border-radius: 12px;
            margin-bottom: 18px;
            font-weight: 600;
        }

        .fail-actions {
            display: flex;
            gap: 10px;
            justify-content: center;
            flex-wrap: wrap;
        }
    </style>
</head>
<body>
    <div class="fail-shell">
        <div class="fail-card">
            <div class="fail-icon">!</div>
            <div class="fail-title">Transaction Failed</div>
            <div class="fail-sub">Please try again or choose another payment method.</div>

            <div class="fail-message">
                <%= message %>
            </div>

            <div class="fail-actions">
                <a href="/payment" class="btn btn-primary">Try Again</a>
                <a href="/cart" class="btn btn-outline-secondary">Back to Cart</a>
            </div>
        </div>
    </div>
</body>
</html>