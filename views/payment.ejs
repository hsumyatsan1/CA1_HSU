<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Payment - Supermarket</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/theme.css">
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --pay-bg: #f6f7fb;
      --pay-card: #ffffff;
      --pay-ink: #1e1f2b;
      --pay-muted: #9096a2;
      --pay-accent: #d4a73f;
      --pay-accent-dark: #121321;
      --pay-line: #e8e9ee;
      --pay-success: #27ae60;
    }

    body {
      background-image: url("https://img.freepik.com/premium-photo/grocery-shopping-online-food-delivery-you-order-purchase-vegetarian-nutrition-food-online-mobile-application-phone-buying-copy-space_36325-3880.jpg");
      background-repeat: no-repeat;
      background-size: cover;
      background-position: center;
      min-height: 100vh;
    }

    .payment-page {
      min-height: calc(100vh - 56px);
      display: flex;
      align-items: center;
      justify-content: flex-start;
      padding: 32px 8vw 48px;
    }

    .payment-shell {
      max-width: 600px;
      margin: 0;
      padding: 0 16px;
    }

    .payment-card {
      background: var(--pay-card);
      border-radius: 30px;
      box-shadow: 0 25px 55px rgba(17, 24, 39, 0.15);
      padding: 32px 28px 28px;
      border: 1px solid #f0f1f5;
    }

    .payment-title {
      font-size: 1.25rem;
      font-weight: 700;
      margin-bottom: 16px;
    }

    .method-list {
      display: grid;
      gap: 12px;
      margin-bottom: 18px;
    }

    .method-item {
      position: relative;
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 12px 14px;
      border: 1px solid var(--pay-line);
      border-radius: 16px;
      background: #fff;
      cursor: pointer;
      transition: border-color 0.2s ease, box-shadow 0.2s ease;
    }

    .method-item:hover {
      border-color: #d7dbe6;
      box-shadow: 0 10px 20px rgba(17, 24, 39, 0.06);
    }

    .method-item input {
      position: absolute;
      opacity: 0;
      pointer-events: none;
    }

    .method-badge {
      width: 38px;
      height: 38px;
      border-radius: 12px;
      display: grid;
      place-items: center;
      font-weight: 700;
      font-size: 0.75rem;
      color: #fff;
    }

    .badge-cc { background: #2b2f3f; }
    .badge-pp { background: #1f6feb; }
    .badge-qr { background: #2db784; }

    .method-info {
      flex: 1;
      display: grid;
    }

    .method-name {
      font-weight: 600;
      font-size: 0.95rem;
    }

    .method-meta {
      font-size: 0.8rem;
      color: var(--pay-muted);
    }

    .method-check {
      width: 18px;
      height: 18px;
      border-radius: 50%;
      border: 2px solid #cfd3dd;
      display: inline-grid;
      place-items: center;
    }

    .method-item input:checked ~ .method-check {
      border-color: var(--pay-accent-dark);
      box-shadow: inset 0 0 0 5px var(--pay-accent-dark);
    }

    .promo {
      background: linear-gradient(135deg, #e0b24b, #d4a73f);
      border-radius: 16px;
      padding: 12px;
      display: grid;
      grid-template-columns: 1fr auto;
      align-items: center;
      gap: 10px;
      margin-bottom: 18px;
    }

    .promo input {
      border: none;
      background: transparent;
      color: #1f2330;
      font-weight: 600;
      outline: none;
      padding-left: 6px;
    }

    .promo input::placeholder {
      color: rgba(31, 35, 48, 0.7);
    }

    .promo button {
      background: var(--pay-accent-dark);
      color: #fff;
      border: none;
      border-radius: 12px;
      padding: 8px 16px;
      font-weight: 600;
      cursor: pointer;
    }

    .summary {
      border-top: 1px dashed var(--pay-line);
      border-bottom: 1px dashed var(--pay-line);
      padding: 14px 4px;
      margin-bottom: 18px;
      display: grid;
      gap: 8px;
      font-size: 0.9rem;
    }

    .summary-row {
      display: flex;
      justify-content: space-between;
      color: var(--pay-muted);
    }

    .summary-row strong {
      color: var(--pay-ink);
      font-weight: 700;
    }

    .summary-total {
      font-size: 1rem;
      color: var(--pay-ink);
      font-weight: 700;
    }

    .pay-btn {
      width: 100%;
      background: var(--pay-accent-dark);
      color: #fff;
      border: none;
      border-radius: 14px;
      padding: 12px 16px;
      font-weight: 700;
      letter-spacing: 0.02em;
      margin-bottom: 10px;
    }

    .pay-sub {
      text-align: center;
      font-size: 0.85rem;
      color: var(--pay-muted);
    }

    .pay-sub a {
      color: var(--pay-accent-dark);
      text-decoration: none;
      font-weight: 600;
    }

    .pay-sub a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>

<nav class="navbar navbar-expand-sm bg-dark navbar-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Supermarket</a>
    <ul class="navbar-nav ms-auto">
      <li class="nav-item"><a class="nav-link" href="/cart">Back to Cart</a></li>
      <li class="nav-item"><a class="nav-link" href="/history">History</a></li>
      <li class="nav-item"><a class="nav-link" href="/logout">Logout</a></li>
    </ul>
  </div>
</nav>

<% const baseTotal = parseFloat(total || 0); %>
<% const additionalCost = baseTotal > 0 ? 0.5 : 0; %>
<% const grandTotal = baseTotal + additionalCost; %>

<div class="payment-page">
  <div class="payment-shell">
    <% if (messages && messages.error && messages.error.length) { %>
      <div class="alert alert-danger">
        <% messages.error.forEach(msg => { %>
          <p><%= msg %></p>
        <% }); %>
      </div>
    <% } %>
    <% if (messages && messages.success && messages.success.length) { %>
      <div class="alert alert-success">
        <% messages.success.forEach(msg => { %>
          <p><%= msg %></p>
        <% }); %>
      </div>
    <% } %>

    <div class="payment-card">
      <div class="payment-title">Select Payment Method</div>

      <form method="POST" action="/payment" id="paymentForm">
        <div class="method-list">
          <label class="method-item">
            <input type="radio" id="credit-card" name="paymentMethod" value="credit-card">
            <span class="method-badge badge-cc">CC</span>
            <span class="method-info">
              <span class="method-name">Credit Card</span>
              <span class="method-meta">**** 2401</span>
            </span>
            <span class="method-check"></span>
          </label>

          <label class="method-item">
            <input type="radio" id="paypal" name="paymentMethod" value="paypal">
            <span class="method-badge badge-pp">PP</span>
            <span class="method-info">
              <span class="method-name">PayPal</span>
              <span class="method-meta">user@paypal.com</span>
            </span>
            <span class="method-check"></span>
          </label>

          <label class="method-item">
            <input type="radio" id="qr-code" name="paymentMethod" value="qr-code">
            <span class="method-badge badge-qr">QR</span>
            <span class="method-info">
              <span class="method-name">NETS QR</span>
              <span class="method-meta">Scan to pay</span>
            </span>
            <span class="method-check"></span>
          </label>
        </div>

        <div class="summary">
          <div class="summary-row">
            <span>Transfer Amount</span>
            <strong>$<%= baseTotal.toFixed(2) %></strong>
          </div>
          <div class="summary-row summary-total">
            <span>Total</span>
            <span>$<%= baseTotal.toFixed(2) %></span>
          </div>
        </div>

        <button type="submit" class="pay-btn">Pay</button>
        <div class="pay-sub">
          Need to edit your cart? <a href="/cart">Back to Cart</a>
        </div>
      </form>
    </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

<script>
  document.getElementById('paymentForm').addEventListener('submit', function(e) {
    const selectedMethod = document.querySelector('input[name="paymentMethod"]:checked');

    if (!selectedMethod) {
      e.preventDefault();
      alert('Please select a payment method');
      return false;
    }
    return true;
  });
</script>

</body>
</html>
