<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Transaction Successful</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --ok-bg: #f6f7fb;
            --ok-card: #ffffff;
            --ok-ink: #1f2330;
            --ok-muted: #6b7280;
            --ok-accent: #22c55e;
            --ok-line: #e5e7eb;
        }

        body {
            font-family: "Manrope", "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
            background: radial-gradient(900px 400px at 10% 0%, #dcfce7 0%, transparent 60%),
                        radial-gradient(800px 300px at 100% 0%, #e8edff 0%, transparent 65%),
                        var(--ok-bg);
            color: var(--ok-ink);
        }

        .ok-shell {
            min-height: 100vh;
            display: grid;
            place-items: center;
            padding: 32px 16px 48px;
        }

        .ok-card {
            background: var(--ok-card);
            border-radius: 20px;
            padding: 28px 24px;
            box-shadow: 0 24px 50px rgba(17, 24, 39, 0.12);
            border: 1px solid var(--ok-line);
            max-width: 520px;
            width: 100%;
            text-align: center;
        }

        .ok-icon {
            width: 84px;
            height: 84px;
            margin: 0 auto 14px;
            display: grid;
            place-items: center;
            border-radius: 22px;
            background: rgba(34, 197, 94, 0.12);
            color: var(--ok-accent);
            font-size: 2.4rem;
            font-weight: 700;
        }

        .ok-title {
            font-weight: 700;
            font-size: 1.5rem;
            margin-bottom: 6px;
        }

        .ok-sub {
            color: var(--ok-muted);
            margin-bottom: 18px;
        }

        .ok-message {
            background: #f0fdf4;
            border: 1px solid rgba(34, 197, 94, 0.25);
            color: #166534;
            padding: 10px 12px;
            border-radius: 12px;
            margin-bottom: 18px;
            font-weight: 600;
        }

        .ok-actions {
            display: flex;
            gap: 10px;
            justify-content: center;
            flex-wrap: wrap;
        }
    </style>
</head>
<body>
    <div class="ok-shell">
        <div class="ok-card">
            <div class="ok-icon">&#10003;</div>
            <div class="ok-title">Payment Successful</div>
            <div class="ok-sub">Your NETS QR payment has been completed.</div>

            <div class="ok-message">
                <%= message %>
            </div>

            <div class="ok-actions">
                <a href="/shopping" class="btn btn-primary">Continue Shopping</a>
                <a href="/cart" class="btn btn-outline-secondary">View Cart</a>
            </div>
        </div>
    </div>
</body>
</html>